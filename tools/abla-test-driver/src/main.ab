import "../vendor/abla-testrunner/src/testrunner.ab"
import "abla/process/arguments"

fun main: int {
    val arguments: array<string> = []
    var index = 0
    while (index < processArgumentCount()) {
        arguments.append(processArgument(index))
        index = index + 1
    }
    runTestCommand(arguments)
}
