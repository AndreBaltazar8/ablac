#import("abla/build")

// A tiny native value proves that the Android application still contains an
// Abla arm64 library. Server actions are deliberately absent from this slice.
fun initialCounter: int = 0

val exported = #exportFunction("initialCounter", "abla_app_answer")
