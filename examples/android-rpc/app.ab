import "main.ab"
import "abla/build"

// The generated adapter and incrementCounter are unreachable from this JNI
// root and get removed from the Android slice.
fun androidClickArgument: int = initialCounter()

val exported = #exportFunction("androidClickArgument", "abla_app_answer")
