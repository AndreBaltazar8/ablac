import "android_rpc.ab"

@rpc
fun incrementCounter(current: int): int = current + 1

fun initialCounter: int = 0

fun counterApplication: int =
    $androidclick { incrementCounter(initialCounter()) }
