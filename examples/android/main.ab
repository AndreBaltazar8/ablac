import "abla/build"

// This function is compiled into libabla_app.so. The generated Kotlin activity
// calls it through the extension-owned JNI adapter and displays the result.
fun helloValue: int = 42

val exported = #exportFunction(
    "helloValue",
    "abla_app_answer"
)
