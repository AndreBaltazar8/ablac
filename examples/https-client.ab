import "abla/http/tls"

fun main: int {
    val client = httpsClient("example.com")
    val result = client.send(httpGet("/"))
    if (result.succeeded && result.complete &&
        result.response.status == 200) 0 else 1
}
