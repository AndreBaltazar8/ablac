import "abla/http/event"

fun trending(request: HttpRequest): HttpResponse = httpText(
    "event-driven Abla HTTP\n"
)

fun main: int {
    val router = httpRouter()
    router.get("/", trending)
    val server = httpEventServer(router, ipv4Any(8080))
    server.run()
}
