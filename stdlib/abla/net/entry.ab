// Portable network facade. The current production target selects the compact
// raw Linux implementation; future targets provide the same API in their own
// platform module without changing HTTP or application code.

#import("abla/sys/linux/net")
