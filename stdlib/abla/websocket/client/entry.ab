// Client-side WebSocket helpers kept separate so servers do not retain random
// entropy code when they only accept masked peer frames.

import "abla/random"
import "abla/websocket"

fun webSocketClientMask(): SecureRandomResult = secureRandomBytes(4)
