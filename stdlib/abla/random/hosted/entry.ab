// Hosted spelling for the all-Abla secure-random implementation. Keeping the
// adapter as a forwarding module preserves the portable API without retaining
// a second allocator, retry loop, or host-runtime entry point.

import "abla/random"

fun hostedSecureRandomBytes(count: int): SecureRandomResult =
    secureRandomBytes(count)
