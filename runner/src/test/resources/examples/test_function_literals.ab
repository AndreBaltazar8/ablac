#import("externals_common.ab")

fun test: int {
    {
        printf("Hi\n")
    }()

    testArg { arg: int ->
        arg * 2
    }
}

fun testArg(argFn: (int) -> int): int {
    argFn(6) + 5
}

fun main: int {
    #test()
    test()
}