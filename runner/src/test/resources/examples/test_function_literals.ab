#import("externals_common.ab")

fun test {
    {
        printf("Hi\n")
    }()
}

fun testArg(argFn: (int) -> int): int {
    argFn(6) + 5
}

fun main: int {
    #test()
    test()
    testArg { arg: int ->
        arg * 2
    }
}