// entry point for abla math library

fun min(a: int, b: int): int = if (b > a) a else b

fun max(a: int, b: int): int = if (a > b) a else b

fun clamp(value: int, min: int, max: int): int {
    val t = if (value < min) min else value
    if (t > max) max else t
}
