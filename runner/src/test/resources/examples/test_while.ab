#import("externals_common.ab")

fun test() {
    var a = 0
    while (a < 80) {
        printf("#")
        a = a + 1
    }
    printf("\n")

    var b = 0
    do {
        printf("#")
        b = b + 1
    } while(b < 80)
    printf("\n")
}

fun main {
    #test()
    test()
}