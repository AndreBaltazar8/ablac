#import("externals_common.ab")
#import("stdlib.ab")

compiler fun testNull(a: int?): int? {
    if (a == null)
        null
    else
        a + 10
}

compiler fun test {
    if (testNull(1) == 11) printf("Ok non null\n")
    if (testNull(null) == null) printf("Ok null\n")
}

// TODO: implement null functionality at run time
fun main {
    #test()
}