import "abla/sys/linux/fs"

fun main(): int {
    val root = "build/runtime-path"
    val index = 0
    val value = Path("$root/$index.path")
    val parent = value.parent().text
    val written = value.writeTextAtomic("probe")
    value.remove()
    if (parent == root && written) 42 else 1
}
