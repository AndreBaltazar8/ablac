import "abla/build"
import "abla/unsafe/native"

fun initialValue(): int = 41

var initializedValue: int = initialValue()

trusted fun freestandingEntry(): int {
    nativeInitializeGlobals()
    initializedValue + 1
}

#exportFunction("freestandingEntry", "freestanding_entry")
