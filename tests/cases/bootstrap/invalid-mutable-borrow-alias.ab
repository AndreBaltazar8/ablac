resource class Counter(var value: int)

fun copy(var target: Counter, source: Counter): int {
    target.value = source.value
    target.value
}

fun main: int {
    val counter = Counter(42)
    copy(counter, counter)
}
