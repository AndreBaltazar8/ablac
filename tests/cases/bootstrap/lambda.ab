fun increment(value: int): int = value + 1

fun apply(function: (int) -> int): int = function(4)

fun main: int {
    val referenced = apply(increment)
    val trailing = apply { value: int -> value + 38 }
    if (referenced == 5) trailing else 0
}
