resource class Ticket(val value: int)

fun main: int {
    val left = Shared(Ticket(20))
    val right = Shared(Ticket(22))
    val view = when (true) {
        true -> left.get()
        false -> right.get()
    }
    view.value
}
