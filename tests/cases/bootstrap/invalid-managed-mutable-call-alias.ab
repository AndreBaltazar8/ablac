class Counter(var value: int)

fun copyValue(var target: Counter, source: Counter): int {
    target.value = source.value
    target.value
}

fun main: int {
    val counter = Counter(1)
    copyValue(counter, counter)
}
