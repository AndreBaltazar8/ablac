resource class Ticket(val value: int)

class TicketBox(var ticket: Ticket) {
    fun overlap(own consumed: Ticket): int =
        this.ticket.value + consumed.value
}

fun main: int {
    val box = TicketBox(Ticket(21))
    box.overlap(move(box.ticket))
}
