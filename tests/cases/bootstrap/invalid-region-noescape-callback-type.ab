fun ordinaryCallback(value: string): int = value.size

fun requireNoEscape(
    callback: FnNoEscape(string) -> int
): int = callback("value")

fun main: int = if (requireNoEscape(ordinaryCallback) == 5) 5 else 1
