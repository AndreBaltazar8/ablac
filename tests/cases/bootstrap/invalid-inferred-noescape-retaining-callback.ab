extern:"c" fun retainCallbackValue(value: string): void

fun retainingCallback(value: string): int {
    retainCallbackValue(value)
    value.size
}

fun requireNoEscape(
    callback: FnNoEscape(string) -> int
): int = callback("value")

fun main: int = requireNoEscape(retainingCallback)
