resource class Ticket(val value: int)

class TicketBox(val ticket: Ticket)

fun main: int {
    val owner = Shared(Ticket(42))
    val view = owner.get()
    val escaped = TicketBox(view)
    escaped.ticket.value
}
