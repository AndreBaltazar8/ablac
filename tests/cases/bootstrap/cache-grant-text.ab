// Mentioning an API name in ordinary source is not a staged capability
// request and must not disable the native object cache.
fun marker(): string = "compilerGrant("

fun main: int = if (marker().size == 14) 42 else 0
