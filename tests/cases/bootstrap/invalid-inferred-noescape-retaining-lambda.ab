extern:"c" fun retainLambdaValue(value: string): void

fun requireNoEscape(
    callback: FnNoEscape(string) -> int
): int = callback("value")

fun main: int = requireNoEscape({ value: string ->
    retainLambdaValue(value)
    value.size
})
