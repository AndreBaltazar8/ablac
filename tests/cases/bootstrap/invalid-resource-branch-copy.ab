resource class Ticket(val value: int)

fun choose(condition: bool): int {
    val source = Ticket(42)
    val destination = if (condition) source else Ticket(0)
    destination.value
}

fun main: int = choose(true)
