var retained: string = ""

fun requireNoEscape(
    callback: FnNoEscape(string) -> int
): int = callback("value")

fun main: int = requireNoEscape({ value: string ->
    retained = value
    value.size
})
