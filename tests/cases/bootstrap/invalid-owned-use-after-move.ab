resource class Ticket(val value: int)

fun consume(own ticket: Ticket): int = ticket.value

fun consumeTwice(own ticket: Ticket): int {
    val first = consume(move(ticket))
    first + ticket.value
}

fun main: int = consumeTwice(Ticket(42))
