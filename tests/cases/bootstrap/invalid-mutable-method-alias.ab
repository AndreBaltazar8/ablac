resource class Counter(var value: int) {
    fun copy(source: Counter): int {
        this.value = source.value
        this.value
    }
}

fun main: int {
    val counter = Counter(42)
    counter.copy(counter)
}
