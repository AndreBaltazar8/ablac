class Counter(var value: int)

class CounterPair(val left: Counter, val right: Counter)

fun main: int {
    val left = Counter(20)
    val right = Counter(22)
    val pair = CounterPair(left, right)
    pair.left.value + pair.right.value
}
