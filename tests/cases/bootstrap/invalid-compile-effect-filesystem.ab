noescape extern:"host" fun ablaHostReadFile(path: string): string

fun readBuildInput(path: string): string = ablaHostReadFile(path)

compile fun loadBuildInput(): int = readBuildInput("secret.txt").size

val loaded = #loadBuildInput()

fun main: int = loaded
