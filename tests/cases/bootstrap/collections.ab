fun main: int {
    val values = [19]
    values.append(23)
    val text = "AZ"
    if (
        values.size == 2 &&
        values[0] + values[1] == 42 &&
        text.size == 2 &&
        text[1] == 90 &&
        text.slice(0, 1) == "A"
    ) 42 else 0
}
