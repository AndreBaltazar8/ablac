resource class Ticket(val value: int)

compile fun invalidBorrowLifetime(): int {
    val owner = Shared(Ticket(42))
    val view = owner.get()
    val moved = move(owner)
    view.value
}

val answer = #invalidBorrowLifetime()

fun main: int = answer
