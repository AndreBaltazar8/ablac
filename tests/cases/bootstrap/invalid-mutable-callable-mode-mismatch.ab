resource class Counter(var value: int)

fun increment(var counter: Counter): int {
    counter.value = counter.value + 1
    counter.value
}

fun invokeShared(callback: (Counter) -> int, counter: Counter): int =
    callback(counter)

fun main: int {
    val counter = Counter(41)
    invokeShared(increment, counter)
}
