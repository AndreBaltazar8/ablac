class Counter(var value: int) {
    fun increment(by: int = 1): int {
        value = value + by
        value
    }
}

compile fun makeAnswer(): int = Counter(40).increment(2)

fun main: int = #makeAnswer()
