fun invokeUnknown(
    callback: (string) -> int,
    value: string
): int = callback(value)

fun observe(value: string): int = value.size

fun main: int = region {
    invokeUnknown(observe, "region allocation")
    42
}
