class Item(val value: int)
class Store(val items: array<Item>)

val state: Store = Store([])

fun main: int {
    state.items.append(Item(42))
    if (state.items.size == 1) state.items[0].value else 0
}
