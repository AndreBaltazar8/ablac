fun choose(value: int): int {
    var adjusted = value
    while (adjusted < 42) {
        adjusted = adjusted + 1
    }
    if (adjusted == 42) adjusted else 0
}

fun main: int {
    val initial = 39
    choose(initial)
}
