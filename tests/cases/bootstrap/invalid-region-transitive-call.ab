extern:"c" fun retainRegionTransitively(value: string): void

fun retainWrapper(value: string): void = retainRegionTransitively(value)

fun main: int = region {
    retainWrapper("cannot escape transitively")
    42
}
