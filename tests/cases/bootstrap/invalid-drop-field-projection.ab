resource class Token(val amount: int)

resource class DropBox(val token: Token) {
    fun drop(): void {
        return
    }
}

fun invalid(): Token {
    var box = DropBox(Token(1))
    move(box.token)
}
