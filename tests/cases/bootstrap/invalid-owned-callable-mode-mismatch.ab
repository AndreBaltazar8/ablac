resource class Ticket(val value: int)

fun consume(own ticket: Ticket): int = ticket.value
fun invokeBorrowed(callback: (Ticket) -> int, ticket: Ticket): int =
    callback(ticket)

fun main: int {
    val ticket = Ticket(42)
    invokeBorrowed(consume, ticket)
}
