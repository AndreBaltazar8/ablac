resource class Ticket(val value: int)

fun inspect(flag: bool): int {
    val owner = Shared(Ticket(42))
    val view = owner.get()
    if (flag) {
        val moved = move(owner)
        moved.get().value
    }
    view.value
}

fun main: int = inspect(false)
