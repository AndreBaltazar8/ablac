class RegionMethodEscapeHolder(var value: string) {
    fun replace(value: string): void {
        this.value = value
        return
    }
}

fun main: int {
    val escaped = RegionMethodEscapeHolder("before")
    region {
        escaped.replace("region allocation")
        42
    }
}
