class NullableFieldBox(val value: int)

class NullableFieldHolder(var value: NullableFieldBox?)

fun read(holder: NullableFieldHolder): int =
    if (holder.value != null) holder.value.value else 0

fun main: int = read(NullableFieldHolder(NullableFieldBox(42)))
