resource class Ticket(val value: int)

fun overlap(borrowed: Ticket, own consumed: Ticket): int =
    borrowed.value + consumed.value

fun main: int {
    val ticket = Ticket(21)
    overlap(ticket, move(ticket))
}
