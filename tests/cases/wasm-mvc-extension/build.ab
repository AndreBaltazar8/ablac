import "abla/compiler"
import "abla/mvc/wasm/build"

#compilerGrant("filesystem.write")

val built = #mvcBuildWasmModule(
    "abla-mvc-browser",
    "tests/cases/wasm-mvc-extension/app.ab",
    "build/wasm-mvc-extension",
    true,
    true
)

fun main: int = if (built) 36 else 1
