trusted noescape extern:"c" fun micro_platform_call(
    request: int,
    requestLength: int,
    response: int,
    responseCapacity: int
): int

@export("abla_wasm_platform_probe")
trusted fun platformProbe(pointer: u32): int =
    micro_platform_call(pointer, 0, pointer, 0)
