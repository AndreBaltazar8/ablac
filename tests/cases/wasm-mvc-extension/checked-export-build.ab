#import("abla/wasm/build")

val built = #wasmBuildModule(
    "unsupported-checked-export",
    "tests/cases/wasm-mvc-extension/checked-export.ab",
    "build/wasm-mvc-extension/checked-export.wasm",
    true,
    false
)

fun main: int = if (built) 0 else 1
