#import("abla/wasm/build")

val built = #wasmBuildModule(
    "unsupported-owned-result",
    "tests/cases/wasm-mvc-extension/owned-result.ab",
    "build/wasm-mvc-extension/owned-result.wasm",
    true,
    false
)

fun main: int = if (built) 0 else 1
