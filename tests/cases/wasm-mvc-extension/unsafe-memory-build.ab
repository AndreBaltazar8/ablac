import "abla/wasm/build"

val built = #wasmBuildModule(
    "unsafe-memory",
    "tests/cases/wasm-mvc-extension/unsafe-memory.ab",
    "build/wasm-mvc-extension/unsafe-memory.wasm",
    true,
    false
)

fun main: int = if (built) 0 else 1
