var exportInitializationValues = [40, 2]

@export("abla_guardless_add")
fun guardlessAdd(left: int, right: int): int = left + right

fun statefulValue(): int = exportInitializationValues[0] +
    exportInitializationValues[1]

@export("abla_guarded_value")
fun guardedValue(): int = statefulValue()
