#import("abla/build")

fun answer: int = 42
fun add(value: int, delta: int): int = value + delta

val exported = #exportFunction(
    "answer",
    "abla_app_answer"
)

val addExported = #exportFunction(
    "add",
    "abla_app_add"
)
