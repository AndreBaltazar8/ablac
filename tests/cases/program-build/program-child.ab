#import("abla/compiler")

val grandchildScheduled = #compilerBuildProgram(
    "program-grandchild",
    "tests/cases/program-build/program-grandchild.ab",
    "build/programmable-build/program-grandchild",
    "x86_64-linux",
    "executable",
    true,
    true
)

fun main: int = if (grandchildScheduled) 41 else 1
