# Portable helper shared by multiple clients and native code.
fun portableClientAdder(value: int): int = value + 1

@client
fun statefulClientInc(value: int): int = portableClientAdder(value)

@client
fun unselectedStatefulClient(value: int): int = portableClientAdder(value) + 10
