import "abla/compiler"

val childScheduled = #compilerBuildProgram(
    "program-child",
    "tests/cases/program-build/program-child.ab",
    "build/programmable-build/program-child",
    "x86_64-linux",
    "executable",
    true,
    true
)

fun main: int = if (childScheduled) 40 else 1
