var scalarExportCounter = 40

@export("abla_scalar_next")
fun scalarNext(): int {
    scalarExportCounter = scalarExportCounter + 1
    scalarExportCounter
}

@export("abla_scalar_add_u32")
fun scalarAddU32(left: u32, right: u32): u32 = left + right
