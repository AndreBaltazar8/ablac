import "abla/build"

val target = #defineTarget(
    "invalid-mobile-target",
    "aarch64-example-none",
    "unsupported-format",
    "lld-elf",
    "aarch64linux",
    false,
    false
)

fun main: int = if (target) 0 else 1
