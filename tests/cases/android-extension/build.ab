import "abla/android/build"
import "abla/compiler"

#compilerGrant("filesystem.write")

val built = #androidBuildArm64Library(
    "abla-android-app",
    "tests/cases/android-extension/app.ab",
    "build/android-extension",
    "org.abla.generated",
    true,
    true
)

fun main: int = if (built) 37 else 1
