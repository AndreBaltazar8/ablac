import "abla/compiler"

#compilerGrant("environment")

fun main: int {
    val value = #compilerEnvironment("ABLA_COMPILE_TEST_INPUT")
    if (value == "first") 42 else if (value == "second") 7 else 1
}
