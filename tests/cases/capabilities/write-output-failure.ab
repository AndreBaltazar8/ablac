import "abla/compiler"

#compilerGrant("filesystem.write")

val staged = #compilerWriteFile(
    "build/compile-effects-test/transaction-output.txt",
    "must-not-be-published\n"
)

fun main: MissingCompileOutputType = 0
