import "abla/compiler"

#compilerGrant("network")

val response = #compilerTcpExchange(
    "127.0.0.1",
    39091,
    "PING",
    1024,
    2000
)

fun main: int = if (response == "PONG") 42 else 1
