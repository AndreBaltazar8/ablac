#import("abla/compiler")
#import("abla/io")

#compilerGrant("filesystem.read")

fun main: int {
    val buildInput = #ablaHostReadFile(
        "tests/cases/capabilities/input.txt"
    )
    if (buildInput == "first\n") 42 else if (buildInput == "second\n") 7 else 1
}
