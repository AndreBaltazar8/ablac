import "../../../examples/android-rpc/android_rpc.ab"

fun localOnlyAction(current: int): int = current + 1

fun invalidApplication: int =
    $androidclick { localOnlyAction(0) }

fun main: int = invalidApplication
