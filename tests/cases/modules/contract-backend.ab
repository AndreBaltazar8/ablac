import "contract-server-support.ab" as support

@rpc
fun remoteIncrement(value: int, bonus: int = 2): support.ContractResult =
    support.ContractResult(support.serverAdd(value, bonus), null)

val serverRuntimeState = support.ServerOnlyRecord(
    support.serverAdd(20, 22)
)
