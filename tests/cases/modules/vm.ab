fun factorial(value: int): int =
    if (value <= 1) 1 else value * factorial(value - 1)

fun main: int {
    var total = 0
    var value = 1
    while (value <= 5) {
        total = total + value
        value = value + 1
    }
    factorial(5) + total
}

