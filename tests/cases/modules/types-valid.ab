fun choose(value: int?): int? = if (value == null) null else value

fun apply(function: (int) -> int): int = function(4)

fun main: int {
    val values: array<int> = [1, 2, 3]
    val next = apply { value: int -> value + 1 }
    if (choose(null) == null) values[1] + next else 0
}

