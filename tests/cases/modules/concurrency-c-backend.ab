#import("../../../src/backend_c.ab")
#import("abla/sys/linux/io")

fun main: int {
    linuxPrintStandardOutput(bootstrapEmitC(
        "fun main: int {\n    var total = 0\n    for (value in generator { yield 20; yield 1 }) { total = total + value }\n    val scheduled = task { 10 }\n    val concurrent = thread { 11 }\n    total + await scheduled + await concurrent\n}\n"
    ))
    0
}
