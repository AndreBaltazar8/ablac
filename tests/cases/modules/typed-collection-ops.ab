fun main(): int {
    val text = "typed"
    val numbers = [10, 20, 30]
    if (text == "typed" && text.size == 5 && text[1] == 121 &&
        numbers.size == 3 && numbers[2] == 30) return 42
    1
}
