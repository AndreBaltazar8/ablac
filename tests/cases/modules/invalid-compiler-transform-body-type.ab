import "abla/compiler"

fun answer(value: int): int = value + 1

compile fun breakAnswer(): void {
    compiler.replaceFunctionBody(
        compiler.findFunction("answer"),
        "\"not an integer\""
    )
    return
}

#compiler.transform(breakAnswer)

fun main: int = answer(40)
