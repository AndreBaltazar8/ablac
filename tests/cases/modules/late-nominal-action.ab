import "late-nominal-provider.ab"

class LateNominalResult(val value: int)

fun lateNominalAction(value: int): LateNominalResult =
    LateNominalResult(value)

fun main: int {
    val direct = $lateNominal(lateNominalAction(1))
    generatedLateNominalAdapter(41).value + direct.value
}
