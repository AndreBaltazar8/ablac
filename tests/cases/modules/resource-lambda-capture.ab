resource class Ticket(val value: int)

fun main: int {
    var ticket = Ticket(40)
    val callback = move { ticket.value + 2 }
    ticket = Ticket(0)
    val answer = callback()
    if (answer == 42 && ticket.value == 0) 42 else 0
}
