#import("abla/net")

fun main: int {
    val listener = tcpListen(0)
    val port = listener.localPort()
    val closed = listener.close()
    if (listener.descriptor >= 0 && port > 0 && closed) 42 else 1
}
