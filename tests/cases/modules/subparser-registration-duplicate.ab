#import("abla/compiler/parser")

compile fun parseDuplicate(cursor: ParserCursor): SyntaxExpression {
    syntaxInteger(42)
}

#compilerRegisterSubparser("duplicate", parseDuplicate)
#compilerRegisterSubparser("duplicate", parseDuplicate)

fun main(): int = $duplicate ignored
