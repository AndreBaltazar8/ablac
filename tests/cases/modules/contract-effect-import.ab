import "contract-provider.ab"
import contract "contract-effect-backend.ab" as backend

val contractMarker = $contractCall(backend.remoteIncrement(40))

fun main: int = generatedContractMetadata() + contractMarker
