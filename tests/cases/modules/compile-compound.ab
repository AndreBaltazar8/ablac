class Pair(val left: int, val right: int)

compile fun makeValues(): array<int> = [10, 20, 30]

compile fun makePair(): Pair = Pair(7, 8)

fun main: int {
    val values = #makeValues()
    val pair = #makePair()
    values[1] + pair.left + pair.right
}
