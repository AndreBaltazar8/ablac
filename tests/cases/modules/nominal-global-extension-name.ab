type Pin = int
type Device = int

fun pin(number: int): Pin = Pin(number)
fun Device.pin(): int = 99
fun Pin.asButton(): int = this + 1

fun main: int {
    if (pin(41).asButton() == 42 && Device(1).pin() == 99) 42 else 0
}
