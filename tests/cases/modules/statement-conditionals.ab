fun observe(flag: bool): void {
    if (flag) {
        1
    } else {
        "ignored"
    }
}

fun choose(flag: bool): int {
    val base = 40
    if (flag) {
        base + 2
    } else {
        base + 1
    }
}

fun main(): int {
    observe(true)
    choose(true)
}
