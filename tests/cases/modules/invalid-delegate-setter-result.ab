class ReturningSetter(var value: int) {
    fun getValue(): int = value
    fun setValue(next: int): int {
        value = next
        next
    }
}

fun main: int {
    var delegated: int by ReturningSetter(1)
    delegated
}
