class IntDelegate(var value: int) {
    fun getValue(): int = value
    fun setValue(next: int): void {
        value = next
        return
    }
}

fun main: int {
    var delegated by IntDelegate(1)
    val alias = ^delegated
    ^delegated = IntDelegate(2)
    alias.value
}
