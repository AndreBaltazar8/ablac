import "contract-server-support.ab" as support
import "contract-server-database-does-not-exist.ab"
import "abla/compiler"

#compilerGrant("process.io")

@rpc
fun remoteIncrement(value: int, bonus: int = 2): support.ContractResult =
    support.ContractResult(value + bonus, null)
