import "nested-deferred-nominal-provider.ab"

fun main: int {
    val marker = $nestedDeferredNominal
        <button onclick={nestedDeferredNominalAction(1)}><span></span></button>
    generatedNominalAdapter(42).value + marker
}
