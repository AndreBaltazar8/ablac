fun setLevel(pin: i32, level: u32): i32 =
    if (pin == 4 && level == 1) 0 else 1

fun restore(pin: int, high: bool): bool {
    var restored = true
    if (setLevel(pin, if (high) 1 else 0) != 0) restored = false
    restored
}

fun main: int = if (restore(4, true)) 42 else 1
