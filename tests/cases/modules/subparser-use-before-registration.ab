import "abla/compiler/parser"

fun main(): int = $later ignored

compile fun parseLater(cursor: ParserCursor): SyntaxExpression {
    syntaxInteger(42)
}

#compilerRegisterSubparser("later", parseLater)
