import "abla/compiler"

class EnvironmentDelegate {
    fun getValue(): string = compilerEnvironment("ABLA_DELEGATE_TEST")
}

compile fun readDelegatedEnvironment(): int {
    val value by EnvironmentDelegate()
    value.size
}

val result = #readDelegatedEnvironment()

fun main: int = result
