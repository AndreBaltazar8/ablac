import "abla/json"

fun main: int {
    val frozen = #$json {"number": 20, "name": "abla"}
    val runtime = $json {"number": 20, "nested": [true, {"name": "abla"}]}
    if (frozen.getString("name") == runtime.at(1).at(1).getString("name")) {
        frozen.getInt("number") + runtime.getInt("number") + 2
    } else 0
}
