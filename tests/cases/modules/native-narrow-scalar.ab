trusted noescape extern:"c" fun narrowSignedStatus(): i32
trusted noescape extern:"c" fun narrowUnsignedValue(): u8

trusted fun main: int {
    if (narrowSignedStatus() == 0 && narrowUnsignedValue() == 255) 42 else 1
}
