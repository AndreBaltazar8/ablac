type Port = int

val fixedPort = Port(42)
val registerBase = 100
val registerStatus = registerBase + 28
var mutablePort = Port(1)

fun main: int {
    mutablePort = Port(mutablePort + 1)
    if (fixedPort == 42 && registerStatus == 128 &&
        mutablePort == 2) 42 else 1
}
