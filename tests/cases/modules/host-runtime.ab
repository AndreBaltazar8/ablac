import "abla/fs"
import "abla/process/arguments"

fun main: int {
    val source = path("tests/cases/bootstrap/value.ab").readText()
    if (
        processArgumentCount() == 0 &&
        source.size == 20 &&
        source.slice(0, 3) == "val"
    ) 42 else 0
}
