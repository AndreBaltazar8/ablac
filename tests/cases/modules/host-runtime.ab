#import("abla/io")
#import("abla/process")

fun main: int {
    val source = ablaHostReadFile("tests/cases/bootstrap/value.ab")
    if (
        ablaHostArgumentCount() == 0 &&
        source.size == 20 &&
        source.slice(0, 3) == "val"
    ) 42 else 0
}
